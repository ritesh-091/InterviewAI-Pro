PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/interviewai_pro
JWT_SECRET=super_secret_jwt_key_interviewai_pro_2026
JWT_EXPIRES_IN=7d

# OpenAI Configuration
# If left blank, the application will run in Mock/Simulator fallback mode
OPENAI_API_KEY=

# AI Provider Settings ('gemini' or 'openai')
# If left blank, the application will run in Mock/Simulator fallback mode
AI_PROVIDER=gemini
GEMINI_API_KEY=

# Email SMTP Configuration (Optional, falls back to logging to console)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@interviewaipro.com

# Client Origin (CORS)
CLIENT_URL=http://localhost:5173
